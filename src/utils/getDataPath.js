// utils/getDataPath.js
const getDataPath = (path) => {
  // Return the path as-is or modify it based on your needs
  // e.g., return process.env.PUBLIC_URL + path;
  return path;
};

export default getDataPath;